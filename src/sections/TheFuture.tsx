import { AnimatePresence, motion, useReducedMotion } from 'framer-motion'
import { LockKeyhole, X } from 'lucide-react'
import { useState } from 'react'
import { futureMemories } from '../data/messages'
export function TheFuture({ onAdvance, onBack }: { onAdvance: () => void; onBack: () => void }) {
  const [open, setOpen] = useState<string | null>(null); const reduceMotion = useReducedMotion()
  return <main className="scene chapter-scene future-scene"><p className="eyebrow">Chapter 12 · The Future</p><h1>We’ve shared so much…<br /><em>without ever being in the same place.</em></h1><div className="future-copy"><p>We have heard each other’s voices.</p><p>We have created memories.</p><p>But there is one memory still waiting for us.</p><h2>The day we finally meet.</h2></div><div className="future-cards">{futureMemories.map((memory) => <button key={memory.id} onClick={() => setOpen(memory.id)}><LockKeyhole /><span>{memory.title}</span><small>Locked</small></button>)}</div><p className="future-closing">Until then, we keep writing the story.</p><div className="chapter-actions"><button className="text-button compact" onClick={onBack}>Back</button><button className="primary-button" onClick={onAdvance}>One promise</button></div><AnimatePresence>{open && <motion.div className="card-dialog-backdrop" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={() => setOpen(null)}><motion.section className="card-dialog" role="dialog" aria-modal="true" aria-labelledby="future-title" initial={reduceMotion ? false : { opacity: 0, scale: .95 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0 }} onClick={(event) => event.stopPropagation()}><button autoFocus className="dialog-close" onClick={() => setOpen(null)} aria-label="Close future memory"><X /></button><p className="eyebrow">Future memory</p><h2 id="future-title">Not yet.</h2><p>But someday.</p><button className="text-button compact" onClick={() => setOpen(null)}>Close</button></motion.section></motion.div>}</AnimatePresence></main>
}