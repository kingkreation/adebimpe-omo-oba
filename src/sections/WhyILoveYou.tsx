import { AnimatePresence, motion, useReducedMotion } from 'framer-motion'
import { Heart, X } from 'lucide-react'
import { useState } from 'react'
import { loveReasons } from '../data/messages'
export function WhyILoveYou({ onAdvance, onBack }: { onAdvance: () => void; onBack: () => void }) {
  const [open, setOpen] = useState<number | null>(null); const reduceMotion = useReducedMotion(); const reason = open === null ? null : loveReasons[open]
  return <main className="scene chapter-scene love-scene"><p className="eyebrow">Chapter 08 · Why I Love You</p><h1>There are things about you<br /><em>I keep coming back to.</em></h1><div className="love-orbit">{loveReasons.map((item, index) => <motion.button key={item.id} className="love-token" onClick={() => setOpen(index)} initial={reduceMotion ? false : { opacity: 0, scale: .8 }} animate={{ opacity: 1, scale: 1 }} transition={{ delay: index * .09 }}><Heart size={15} /><span>{item.title}</span></motion.button>)}</div><div className="love-ending"><p>Maybe I could list a hundred reasons.</p><p>But somehow, they all lead back to one thing.</p><h2>I love who you are.</h2></div><div className="chapter-actions"><button className="text-button compact" onClick={onBack}>Back</button><button className="primary-button" onClick={onAdvance}>A playful question</button></div><AnimatePresence>{reason && <motion.div className="card-dialog-backdrop" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={() => setOpen(null)}><motion.section role="dialog" aria-modal="true" aria-labelledby="reason-title" className="card-dialog" initial={reduceMotion ? false : { opacity: 0, scale: .95 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0 }} onClick={(event) => event.stopPropagation()}><button autoFocus className="dialog-close" onClick={() => setOpen(null)} aria-label="Close reason"><X /></button><Heart className="dialog-crown" /><h2 id="reason-title">{reason.title}</h2><p>{reason.body}</p><button className="text-button compact" onClick={() => setOpen(null)}>Close</button></motion.section></motion.div>}</AnimatePresence></main>
}